const {sequilize} = require("../../config/mysql");
const {DataTypes} = require("sequilize");

const Estado =  sequilize.define(
    "Estado",{
        nombre:{
            type: DataTypes.STRING,
            allowNull:false
        }
    },{
        timestamps: true
    }
);

module.exports.Estado;